# Kubee Web - 환경 변수 설정 템플릿
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 수정하세요

# ===========================================
# 🔐 인증 및 보안 설정
# ===========================================
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ===========================================
# 🤖 AI 서비스 설정
# ===========================================
# OpenAI API 키 (AI 기능 사용 시 필수)
OPENAI_API_KEY=your-openai-api-key-here

# AI 모델 설정
OPENAI_MODEL=gpt-4
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=2000

# ===========================================
# 🗄️ 데이터베이스 설정
# ===========================================
DATABASE_URL=sqlite:///users.db
DATABASE_PATH=./users.db

# ===========================================
# 🌐 서버 설정
# ===========================================
# Flask 백엔드 서버
FLASK_HOST=0.0.0.0
FLASK_PORT=8282
FLASK_DEBUG=false

# React 프론트엔드 서버
REACT_PORT=3000

# ===========================================
# 👤 기본 사용자 계정 설정
# ===========================================
# 관리자 계정 (프로덕션에서는 반드시 변경!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@kubee-web.local

# 기본 사용자 계정
DEFAULT_USER_USERNAME=user
DEFAULT_USER_PASSWORD=user123
DEFAULT_USER_EMAIL=user@kubee-web.local

# ===========================================
# 📁 파일 시스템 설정
# ===========================================
# 스캔 결과 저장 디렉토리
SCAN_RESULTS_DIR=./scan_results
LOG_DIR=./logs

# Git 클론 기본 디렉토리
CLONE_BASE_DIR=/tmp/kube-scan

# ===========================================
# 🔍 스캔 설정
# ===========================================
# 동시 스캔 수 제한
MAX_CONCURRENT_SCANS=3

# 스캔 타임아웃 (초)
SCAN_TIMEOUT=300

# Deep Scan 기본값
DEFAULT_DEEP_SCAN=false

# ===========================================
# 📊 로깅 설정
# ===========================================
LOG_LEVEL=INFO
LOG_FORMAT=json

# ===========================================
# 🚀 개발/프로덕션 모드
# ===========================================
ENVIRONMENT=development
# ENVIRONMENT=production

# ===========================================
# 🔒 보안 설정
# ===========================================
# CORS 설정
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 세션 설정
SESSION_TIMEOUT_HOURS=8

# 비밀번호 정책
MIN_PASSWORD_LENGTH=8
REQUIRE_SPECIAL_CHARS=true

# ===========================================
# 📧 알림 설정 (향후 구현)
# ===========================================
# 이메일 알림 설정
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# 알림 설정
ENABLE_EMAIL_NOTIFICATIONS=false
ENABLE_BROWSER_NOTIFICATIONS=true

# ===========================================
# 🐳 Docker 설정
# ===========================================
# Docker 이미지 태그
DOCKER_IMAGE_TAG=latest

# 컨테이너 리소스 제한
CONTAINER_MEMORY_LIMIT=512m
CONTAINER_CPU_LIMIT=0.5
